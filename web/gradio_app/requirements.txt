# Gradio Web App — Brain Tumor Segmentation
# Cài đặt: pip install -r requirements.txt

# ── Core Gradio ─────────────────────────────────────────────────────────────
gradio>=4.20.0,<5.0.0

# ── Medical Imaging ─────────────────────────────────────────────────────────
nibabel>=5.0.0
numpy>=1.24.0
scipy>=1.11.0

# ── Visualization ────────────────────────────────────────────────────────────
matplotlib>=3.7.0
Pillow>=10.0.0

# ── PDF Report ───────────────────────────────────────────────────────────────
fpdf2>=2.7.0,<3.0.0

# ── Model Inference (chọn 1 trong 2) ─────────────────────────────────────────
onnxruntime>=1.18.0               # CPU only (khuyến nghị cho HF Spaces Free)
# onnxruntime-gpu>=1.18.0         # GPU (bỏ comment nếu dùng GPU tier trên HF)

# ── Optional: TorchScript support ────────────────────────────────────────────
# torch>=2.0.0                    # bỏ comment nếu dùng .pt model
